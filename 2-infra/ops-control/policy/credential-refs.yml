---
# Lab credential *names* only. Values do not belong in this repository.
schema_version: 1
stores:
  - name: lab-shared
    fields: [ssh_access_authorized_keys, uptime_admin_email]
  - name: lab-host
    fields: [host-specific placeholders]
notes:
  - Real credential values stay in a private store, not in git.
