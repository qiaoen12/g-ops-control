---
id: linux-cn
host_id: lab-cn-1
kind: server
machine_class: mainland
lifecycle: active
groups: [managed_linux, offload_targets, domestic_hosts, docker_hosts]
actions:
  - {action: host.baseline, expect: allow}
  - {action: software.update, fetches_overseas: true, via_offload: false, expect: deny}
  - {action: software.update, fetches_overseas: true, via_offload: true, expect: allow}
  - {action: backup.restore, backend: legacy-object-storage, expect: deny}
  - {action: service.deploy, service_id: svc-edge-proxy, expect: deny}
