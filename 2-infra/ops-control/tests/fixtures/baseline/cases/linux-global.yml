---
id: linux-global
host_id: lab-global-ops
kind: server
machine_class: global
lifecycle: active
ledger_vs_facts: consistent
groups: [managed_linux, data_ops_hosts, aether_standby_hosts, standard_proxy_hosts]
actions:
  - {action: host.baseline, expect: allow}
  - {action: diagnostics.nodequality, expect: allow}
  - {action: software.check, software_id: sw-ops-core-candidate, ledger_vs_facts: consistent, expect: allow}
