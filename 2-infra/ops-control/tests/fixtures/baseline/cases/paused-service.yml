---
id: paused-service
host_id: lab-global-primary
kind: server
machine_class: global
lifecycle: active
groups: [managed_linux, aether_primary_hosts, xui_hosts]
service_expected: paused
actions:
  - {action: service.start, service_id: svc-ops-core, expect: deny}
  - {action: software.update, software_id: sw-ops-core, expect: deny}
  - {action: site.aggregate, expect: deny}
  - {action: service.deploy, service_id: svc-edge-proxy, entry: standard-proxy, expect: deny}
