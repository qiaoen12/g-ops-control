# lab private examples

This directory tracks `*.example.yml` field names only.
Real `*.secrets.yml` files are gitignored and must not be copied here.
Connection facts do not belong in `inventory/management.yml`.
