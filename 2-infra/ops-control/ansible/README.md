# ansible 投影

默认 inventory 是 `inventories/lab/hosts.ini`（虚构 lab ID + 文档网段）。不要把本目录指回旧生产 `hosts.ini`。

本轮只承接闸门 playbook 与 software policy 投影。18 个 role 未复制，见 `docs/development/source-manifest.md` 的 blocked 项。`site.yml` 保持冻结。
